# 2026.09.25-delta.2 发行验证

- 275 个本体资源 + 1 个可选更新资源，总输出约 70.7 MB；520 份差分均使用包内标准库解码器重建并核对 SHA256。
- 6 个章节标题已核对实际 UV 合成预览。第 1、3、4、5、6 章补齐；第 2 章保留已有译文。只改目标贴图压缩块，容器其他成员内容不变。
- 19 项安装事务/升级测试通过，包含错误版本、缺文件、坏包、符号链接、空间不足、写入失败、突然中断恢复及保护第三方后续修改；5 项 BSDIFF 解码测试通过。
- 在隔离临时资源目录验证：原版安装 276 文件，2026.09.15-delta.1 升级 263 文件，9 月 24 日最终本地版升级 2 文件；三种基线均通过安装、全量目标哈希、重复安装和恢复。
- 真实资源测试只绕过隔离副本的模拟器进程检测；发行安装器仍要求 RPCS3 完全退出。测试没有启动或关闭游戏，也没有操作用户存档。
- 本次全量包沿用已经回读验证的 6,896 条文本、字库及菜单构建，最后新增章节条为图片文字，不包含在上述条数内。

## 未验证

运行测试未完成：新章节标题在所有实际页面的显示、全影片字幕不漏句、START 跳过、闪屏及模型瞬隐。字幕和 START 修订不能视为已证明解决全部故障。

macOS 上完成以上离线安装/恢复验证。Windows/Linux 启动入口未实机测试，PS3 实机未测试。用户明确授权按此已知验证范围正式发布，GitHub Release 不标为预发行。

机器可读结果见 verification.json。发布 ZIP 及校验文件的哈希以 Release 附件为准。
