# 2026.09.25-delta.3 发行验证

- 完整累计包：275 个本体资源及 1 个可选更新资源，523 份差分；新增差分均通过包内解码器重建与 SHA256 核对。
- 升级包：delta.2 → delta.3，仅修改 menu/debriefing.tpf、menu/debriefing.drb、bind/menu/debriefing.000。其余本体资源也验证 delta.2 指纹，防止混装。
- 成功／失败提示改为一个可见元素；第二元素贴图清空，即使动画改动透明度也不会重新出现半句。使用实际解码贴图及原生坐标合成预览核对。
- 结算页 23 个固定文字标签均检查字符映射与字号后的实际宽度；保留 C、AP、按键图标及动态评级／数值。缩短两处过长标题和预计／实际标签，扩大 /AP 单位栏。
- 松散文件与归档成员一致；非目标成员内容保持不变。
- 完整包在隔离目录分别测试原版（276 个文件）、delta.1（263 个文件）、delta.2（3 个文件）的安装、全量哈希、重复安装及恢复，全部通过。
- 小升级包在隔离 delta.2 目录测试安装 3 个文件、重复安装及恢复，全部通过。
- 19 项安装器事务／保护测试和 5 项差分解码测试通过。隔离资源测试仅在临时副本上绕过进程检测；发行安装器保留 RPCS3 运行检测。

## 未验证事项

本轮没有启动游戏。实际任务完成、任务失败、结算页和过渡动画仍须运行验证。正式发布不等于宣称已经完成这些实测。

未改变字幕或 START 实验补丁，不宣称已修复所有漏字幕、跳过失败、菜单闪屏或模型瞬隐。此前的六章标题及其余汉化资源原样保留。

以上离线测试在 macOS 完成，Windows／Linux 入口和 PS3 实机未测试。机器可读结果见 verification.json。
